<%= form_for(@route) do |f| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="list2">
    <div class="field">
      <li>Nombre: </li>
      <li><%= f.text_field :name %></li>
    </div>
    <div class="field">
      <li>Kilometros: </li>
      <li><%= f.number_field :kilometers %></li>
    </div>
    <div class="field">
      <li>Casetas: </li>
      <li><%= f.number_field :booth_price %></li>
    </div>
    <div class="registrar-izq">
      <div class="field">
        <%= submit_tag("Finalizar") %>
        <%= link_to 'Atras', routes_path %>
      </div>
    </div>
  </div>
<% end %>
